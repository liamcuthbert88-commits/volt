node_modules
dist
coverage
.vscode
.DS_Store

# Local VOLT data — durable, but personal, never checked in.
database/*.sqlite
database/*.sqlite-*

# Secrets — API keys never get checked in. .env.example is the template.
.env
.env.*
!.env.example

# npm lockfile — this project uses pnpm (pnpm-lock.yaml); an accidental
# npm install must never commit a conflicting one.
package-lock.json
